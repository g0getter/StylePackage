    import XCTest
    @testable import StylePackage

    final class StylePackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(StylePackage().text, "Hello, World!")
        }
    }
